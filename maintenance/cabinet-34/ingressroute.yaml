apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: funhouse-room-room4
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`riddler.rycr.io`) && PathPrefix(`/r/K41MbXVqh4`)
      kind: Rule
      services:
        - name: funhouse-room-room4
          port: 80
      middlewares:
        - name: funhouse-room-room4-strip
  # Use Traefik entrypoint / default cert store (TLS secret stays in app ns).
  tls: {}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: funhouse-room-room4-strip
spec:
  stripPrefix:
    prefixes:
      - /r/K41MbXVqh4
